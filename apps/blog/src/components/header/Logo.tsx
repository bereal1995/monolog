import styled from '@emotion/styled';

interface Props {}

export default function Logo(props: Props) {

  return (
    <Container>
      LOGO
    </Container>
  )
}
const Container = styled.h1`
  font-weight: 700;
  font-size: 20px;
`;